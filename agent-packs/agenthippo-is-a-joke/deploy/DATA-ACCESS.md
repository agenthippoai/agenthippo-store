<!-- Generated from Agent Pack agenthippo-is-a-joke@0.1.0 — packs.agenthippo.ai -->
# Data & access summary — agenthippo-is-a-joke (compose / edge-fed)

Generated by `agenthippo deploy init` — answers the questions a security review asks first.

## Secrets
- `OPENAI_API_KEY` (or the pack's configured provider key) — your model provider credential

## Egress
- Outbound network access is needed only to `api.anthropic.com:443` (your model provider).
- Identity is verified via the org IdP's own auth-broker over the shared private docker network (`agenthippo-idp-colocate`), not a public network call. The one-time browser login flow (unauthenticated visitors) still redirects to the central IdP's public URL to sign in.

## Blast radius per component
- **agent container** — can reach your model provider and read/write its own state volume. Never holds the JWT signing secret or any OAuth client secret (`.env` vs `.env.auth` split).
- **this deployment holds no auth secrets at all** — identity is verified entirely by the org IdP's own auth-broker (see `agent-route.caddy`); nothing here signs or stores a JWT secret.
